# Legacy integration examples

CampusOS is a Next.js/React application. JavaScript is first-class through the React runtime and `allowJs` TypeScript configuration.

PHP is intentionally kept as a compatibility/reference layer only. Vercel does not execute PHP as part of the Next.js app, so the PHP example documents a future migration path rather than pretending PHP is a supported runtime here.
